# v1_task_manager 核心框架变化分析

**文档版本**: v2  
**生成时间**: 2026-04-22  
**项目路径**: `learn_harness_demo/`

---

## 目录

- [Capiter 1: 基础 Agent Loop](#capiter-1-基础-agent-loop)
- [Capiter 2: 工具系统扩展](#capiter-2-工具系统扩展)
- [Capiter 3: 技能系统引入](#capiter-3-技能系统引入)
- [Capiter 4: 任务管理系统](#capiter-4-任务管理系统)
- [Capiter 5: 子代理系统](#capiter-5-子代理系统)
- [Capiter 6: 上下文管理](#capiter-6-上下文管理)
- [Capiter 7: 权限系统](#capiter-7-权限系统)
- [Capiter 8: Hook 系统](#capiter-8-hook-系统)
- [Capiter 9: 记忆系统](#capiter-9-记忆系统)
- [Capiter 10: 构建系统](#capiter-10-构建系统)
- [Capiter 11: 恢复系统](#capiter-11-恢复系统)
- [Capiter 12: 任务系统增强](#capiter-12-任务系统增强)
- [Capiter 13: v2 后台任务](#capiter-13-v2-后台任务)
- [Capiter 14: Cron 调度器](#capiter-14-cron-调度器)
- [Capiter 18_2: Worktree 隔离](#capiter-18_2-worktree-隔离)
- [Capiter 19_2: MCP 插件](#capiter-19_2-mcp-插件)

---

## Capiter 1: 基础 Agent Loop

**文件路径**: `code/v1_task_manager/chapter_1/agent_loop.py`  
**完整分析**: `docs/zh/chapter_1/s01_agent_loop_文档.md`

### 核心组件

| 组件 | 类型 | 说明 |
|------|------|------|
| `LoopState` | dataclass | 存储对话历史和轮次计数，管理 messages 列表和 turn_count |
| `TOOLS` | list | 工具定义列表，仅包含 bash 工具的 OpenAI function calling 格式定义 |
| `run_one_turn()` | function | 单次 LLM 调用 + 工具执行，返回是否继续循环 |
| `agent_loop()` | function | 主循环入口，持续执行直到模型不再调用工具 |
| `execute_tool_calls()` | function | 解析 LLM 返回的 tool_calls 并执行对应工具 |
| `run_bash()` | function | bash 工具实现，执行 shell 命令并返回输出 |

### 功能改动详情

1. **基础 Agent Loop 结构**
   - 采用 `while run_one_turn(state): pass` 的简洁循环模式
   - LoopState 管理 messages 列表和 turn_count 计数器
   - 每次循环：LLM 调用 → 解析 tool_calls → 执行工具 → 注入结果 → 下一轮

2. **单一工具支持**
   - 仅实现 bash 工具，用于执行 shell 命令
   - 工具定义采用 OpenAI function calling 格式
   - 参数包括 `command`（必填）字符串

3. **简单安全过滤**
   - 危险命令检查：`rm -rf /`、`sudo`、`shutdown`、`reboot` 等
   - 超时保护：120 秒执行超时限制
   - 返回错误信息给模型而非直接抛出异常

4. **交互式入口**
   - `if __name__ == "__main__"` 提供 REPL 式交互
   - 支持 q/exit 退出和空输入退出
   - 支持多轮对话，持续运行直到用户退出或模型完成

5. **模型连接配置**
   - 使用 OpenAI 兼容 API 接口
   - 支持 Thinking 模型（enable_thinking=True）
   - 自动获取可用模型列表

```python
@dataclass
class LoopState:
    messages: list
    turn_count: int = 1
    transition_reason: str | None = None

def run_one_turn(state: LoopState) -> bool:
    response = client.chat.completions.create(            
        model=MODEL, tools=TOOLS, messages=state.messages,
        max_tokens=8000, temperature=1,
        extra_body={"top_k": 20, "chat_template_kwargs": {"enable_thinking": True}}
    )
    response_messages = response.choices[0].message
    state.messages.append(response_messages)
    
    if response_messages.tool_calls:
        results = execute_tool_calls(response_messages)
        for tool_result in results:
            state.messages.append(tool_result)
        state.turn_count += 1
        return True
    return False
```

### 数据结构示例

```python
# LoopState 实例
LoopState(
    messages=[
        {"role": "user", "content": "List files in current directory"},
        {"role": "assistant", "content": None, "tool_calls": [...]},
        {"role": "tool", "content": "file1.txt\nfile2.py", "tool_call_id": "..."}
    ],
    turn_count=2,
    transition_reason=None
)

# TOOLS 定义
TOOLS = [{
    "type": "function",
    "function": {
        "name": "bash",
        "description": "Run a bash command and return the output",
        "parameters": {
            "type": "object",
            "properties": {
                "command": {"type": "string", "description": "The bash command to execute"}
            },
            "required": ["command"]
        }
    }
}]
```

### 与前一章的对比

本章为初始版本，无对比章节。

### 架构图

```
┌──────────┐      ┌───────┐      ┌─────────┐
│   User   │ ───> │  LLM  │ ───> │  Tool   │
│  prompt  │      │       │      │ execute │
└──────────┘      └───┬───┘      └────┬────┘
                      ↑               │
                      │   tool_result │
                      └───────────────┘
                    (loop continues)
```

### 执行流程

```
1. 用户输入 → 添加到 messages
2. 调用 LLM → 获取响应（可能包含 tool_calls）
3. 如果有 tool_calls:
   a. 解析每个 tool_call
   b. 执行对应工具函数
   c. 收集工具输出
   d. 将 tool_result 添加到 messages
4. 重复步骤 2-3，直到 LLM 不再调用工具
5. 返回最终响应给用户
```

### 本章小结

Capiter 1 建立了最基础的 Agent Loop 框架，包含单一 bash 工具支持和简单的安全过滤。循环结构采用经典的 "LLM 决策 → 工具执行 → 结果反馈" 模式，为后续章节的扩展奠定基础。核心设计保持简洁，便于理解和后续扩展。

---

## Capiter 2: 工具系统扩展

**文件路径**: `code/v1_task_manager/chapter_2/s02_tool_use.py`  
**完整分析**: `docs/zh/chapter_2/s02_tool_use_文档_v2.md`

### 核心组件

| 组件 | 类型 | 说明 |
|------|------|------|
| `TOOL_HANDLERS` | dict | 工具名称到处理函数的映射，支持动态扩展 |
| `safe_path()` | function | 路径安全检查，防止目录穿越攻击 |
| `run_read()` | function | read_file 工具实现，读取文件内容 |
| `run_write()` | function | write_file 工具实现，写入文件内容 |
| `run_edit()` | function | edit_file 工具实现，替换文件中的文本 |
| `execute_tool_calls()` | function | 工具调用分发与执行，支持错误处理 |

### 功能改动详情

1. **工具数量扩展**
   - 新增 `read_file`：读取文件内容，支持 limit 参数限制行数
   - 新增 `write_file`：写入文件内容，覆盖已有内容
   - 新增 `edit_file`：编辑文件，替换指定的 old_text 为 new_text
   - 工具总数从 1 个扩展到 4 个

2. **工具分发机制**
   - 引入 `TOOL_HANDLERS` 字典进行工具路由
   - 支持动态扩展新工具而无需修改主循环
   - 未知工具返回错误信息给模型

3. **路径安全保护**
   - `safe_path()` 使用 `is_relative_to()` 检查路径是否在工作目录内
   - 防止目录穿越攻击（如 `../../etc/passwd`）
   - 所有文件操作工具都必须通过此检查

4. **JSON 解析错误处理**
   - 工具参数解析失败时返回错误信息给模型
   - 使用 `continue` 跳过失败的工具调用
   - 模型可以根据错误信息修正参数重新调用

5. **输出长度限制**
   - 工具输出统一限制为 50000 字符
   - 防止过长的输出占用上下文
   - 超出部分被截断并提示模型

```python
TOOL_HANDLERS = {
    "bash":       lambda **kw: run_bash(kw["command"]),
    "read_file":  lambda **kw: run_read(kw["path"], kw.get("limit")),
    "write_file": lambda **kw: run_write(kw["path"], kw["content"]),
    "edit_file":  lambda **kw: run_edit(kw["path"], kw["old_text"], kw["new_text"]),
}

def safe_path(p: str) -> Path:
    """Ensure path is within workspace. Prevent directory traversal."""
    path = (WORKDIR / p).resolve()
    if not path.is_relative_to(WORKDIR):
        raise ValueError(f"Path escapes workspace: {p}")
    return path

def run_read(path: str, limit: int = None) -> str:
    safe_p = safe_path(path)
    content = safe_p.read_text()
    if limit:
        lines = content.splitlines()[:limit]
        content = "\n".join(lines)
    return content
```

### 工具参数定义

```python
# read_file 工具定义
{
    "name": "read_file",
    "parameters": {
        "properties": {
            "path": {"description": "Path to file", "type": "string"},
            "limit": {"description": "Max lines to read", "type": "integer"}
        },
        "required": ["path"],
        "type": "object"
    }
}

# edit_file 工具定义
{
    "name": "edit_file",
    "parameters": {
        "properties": {
            "path": {"description": "Path to file", "type": "string"},
            "old_text": {"description": "Text to replace", "type": "string"},
            "new_text": {"description": "Replacement text", "type": "string"}
        },
        "required": ["path", "old_text", "new_text"],
        "type": "object"
    }
}
```

### 与前一章的对比

| 特性 | Capiter 1 | Capiter 2 |
|------|-----------|-----------|
| 工具数量 | 1 (bash) | 4 (bash + 文件操作) |
| 工具路由 | if-else 硬编码 | TOOL_HANDLERS 字典 |
| 路径安全 | 无 | safe_path() 检查 |
| 错误处理 | 简单 | JSON 解析错误捕获 |
| 输出限制 | 无 | 50000 字符限制 |

### 架构图

```
┌──────────┐      ┌───────┐      ┌──────────────────┐
│   User   │ ───> │  LLM  │ ───> │ Tool Dispatch    │
│  prompt  │      │       │      │ {                │
└──────────┘      └───┬───┘      │   bash: run_bash │
                      ↑          │   read: run_read │
                      │          │   write: run_wr  │
                      │ tool_result                  │
                      │          │   edit: run_edit │
                      └──────────│ }                │
                                 └──────────────────┘
                                        │
                                        ▼
                                 ┌──────────────┐
                                 │ safe_path()  │
                                 │ (validate)   │
                                 └──────────────┘
```

### 错误处理示例

```python
# 参数解析错误
try:
    tool_input = json.loads(tool_call.function.arguments)
except json.JSONDecodeError as e:
    results.append({
        "role": "tool",
        "content": f"Failed to parse arguments: {e}",
        "tool_call_id": tool_call.id
    })
    continue

# 路径安全检查错误
try:
    safe_p = safe_path(tool_input["path"])
except ValueError as e:
    results.append({
        "role": "tool",
        "content": f"Security error: {e}",
        "tool_call_id": tool_call.id
    })
    continue
```

### 本章小结

Capiter 2 在保持 Agent Loop 不变的前提下，通过工具分发字典扩展了工具系统。文件操作工具的引入使 Agent 能够直接读写工作区文件，路径安全检查防止了目录穿越风险。工具分发机制的设计便于后续添加新工具。

---

## Capiter 3: 技能系统引入

**文件路径**: `code/v1_task_manager/chapter_3/s03_skill_loading.py`  
**完整分析**: `docs/zh/chapter_3/s03_skill_loading_文档.md`

### 核心组件

| 组件 | 类型 | 说明 |
|------|------|------|
| `SkillManifest` | dataclass | 技能元数据（name, description, path） |
| `SkillDocument` | dataclass | 技能完整文档（manifest + body） |
| `SkillRegistry` | class | 技能加载与管理，自动扫描 skills 目录 |
| `load_skill` | tool | 动态加载技能内容到上下文 |
| `_parse_frontmatter()` | method | 解析 Markdown frontmatter 元数据 |

### 功能改动详情

1. **技能数据结构**
   - `SkillManifest` 存储技能元数据：name、description、path
   - `SkillDocument` 组合 manifest 和完整内容 body
   - 支持按名称索引和检索

2. **技能注册表**
   - `SkillRegistry` 自动扫描 `skills/` 目录下的 `SKILL.md` 文件
   - 使用 frontmatter 解析技能元数据（YAML 格式）
   - 按技能名称建立索引，支持快速查找

3. **技能加载工具**
   - 新增 `load_skill` 工具供模型按需加载技能
   - 返回格式化的 `<skill name="...">...</skill>` 标签
   - 未知技能返回错误信息和可用技能列表

4. **系统提示注入**
   - 启动时列出所有可用技能到 system prompt
   - 模型可根据任务需求选择加载相关技能
   - 避免一次性注入所有技能占用上下文

5. **文件操作分类**
   - `CONCURRENCY_SAFE = {"read_file"}` 标记安全并发工具
   - `CONCURRENCY_UNSAFE = {"write_file", "edit_file"}` 标记不安全工具
   - 为后续并发控制提供基础

```python
@dataclass
class SkillManifest:
    name: str
    description: str
    path: Path

@dataclass
class SkillDocument:
    manifest: SkillManifest
    body: str

class SkillRegistry:
    def _load_all(self) -> None:
        if not self.skills_dir.exists():
            return
        for path in sorted(self.skills_dir.rglob("SKILL.md")):
            meta, body = self._parse_frontmatter(path.read_text())
            name = meta.get("name", path.parent.name)
            description = meta.get("description", "No description")
            manifest = SkillManifest(name=name, description=description, path=path)
            self.documents[name] = SkillDocument(manifest=manifest, body=body.strip())
    
    def _parse_frontmatter(self, text: str) -> tuple[dict, str]:
        match = re.match(r"^---\n(.*?)\n---\n(.*)", text, re.DOTALL)
        if not match:
            return {}, text
        meta = {}
        for line in match.group(1).strip().splitlines():
            if ":" not in line:
                continue
            key, value = line.split(":", 1)
            meta[key.strip()] = value.strip()
        return meta, match.group(2)
    
    def load_full_text(self, name: str) -> str:
        document = self.documents.get(name)
        if not document:
            known = ", ".join(sorted(self.documents)) or "(none)"
            return f"Error: Unknown skill '{name}'. Available skills: {known}"
        return f'<skill name="{document.manifest.name}">\n{document.body}\n</skill>'
```

### Frontmatter 格式示例

```yaml
---
name: jsonl_handler
description: Best practices for processing JSONL files in Python
---

# JSONL Handler Skill

## Overview
This skill provides patterns for reading and writing JSONL (JSON Lines) files.

## Reading JSONL
```python
with open("data.jsonl") as f:
    for line in f:
        record = json.loads(line)
        process(record)
```

## Writing JSONL
```python
with open("output.jsonl", "w") as f:
    for record in records:
        f.write(json.dumps(record) + "\n")
```
```

### 与前一章的对比

| 特性 | Capiter 2 | Capiter 3 |
|------|-----------|-----------|
| 知识管理 | 无 | SkillRegistry |
| 上下文注入 | 静态 | 动态 load_skill |
| 工具并发标记 | 无 | CONCURRENCY_SAFE/UNSAFE |
| 技能发现 | 无 | 自动扫描 SKILL.md |

### 架构图

```
┌───────────────┐     ┌─────────────┐     ┌──────────┐
│ skills/ dir   │ ──> │ SkillReg    │ <── │ load_skill│
│  └─ SKILL.md  │     │ (registry)  │     │  (tool)  │
│  └─ SKILL.md  │     └──────┬──────┘     └──────────┘
└───────────────┘            │
                             │ describe_available()
                             ▼
                    ┌────────────────┐
                    │ System Prompt  │
                    │ + available    │
                    │   skills list  │
                    └────────────────┘
```

### 技能加载流程

```
1. 模型调用 load_skill(name="jsonl_handler")
2. SkillRegistry 查找对应技能
3. 如果找到：
   - 返回 <skill name="jsonl_handler">...</skill>
4. 如果未找到：
   - 返回错误信息和可用技能列表
5. 结果注入到对话上下文
6. 模型根据技能内容执行任务
```

### 本章小结

Capiter 3 引入了技能系统，允许将特定领域的知识和最佳实践封装为可加载的 SKILL.md 文件。模型可以根据任务需求动态加载相关技能，避免一次性注入过多内容占用上下文。Frontmatter 格式支持结构化的元数据，便于技能分类和检索。

---

## Capiter 4: 任务管理系统

**文件路径**: `code/v1_task_manager/chapter_4/s04_todo_write.py`  
**完整分析**: `docs/zh/chapter_4/s04_todo_write_文档.md`

### 核心组件

| 组件 | 类型 | 说明 |
|------|------|------|
| `PlanItem` | dataclass | 单个计划项（id, content, status, active_form） |
| `PlanningState` | dataclass | 计划状态（items 列表 + rounds_since_update） |
| `TodoManager` | class | 待办任务管理，支持更新和渲染 |
| `task_update` | tool | 更新任务计划 |
| `render()` | method | 格式化输出任务列表 |

### 功能改动详情

1. **任务数据结构**
   - `PlanItem` 包含 id、content、status、active_form 字段
   - status 限定为 `pending`、`in_progress`、`completed`
   - active_form 用于进行时描述（如 "正在读取文件"）

2. **单任务进行中约束**
   - 同一时间最多一个任务处于 `in_progress` 状态
   - 防止任务并行导致的上下文混乱
   - 确保任务执行的有序性

3. **计划更新验证**
   - 限制最多 20 个计划项
   - 验证必填字段（id、content）和有效状态值
   - 返回详细的错误信息供模型修正

4. **轮次追踪**
   - `rounds_since_update` 记录计划未更新的轮次
   - 可用于触发计划审查提醒
   - 帮助模型保持计划的时效性

5. **任务展示格式化**
   - `render()` 方法生成带状态图标的任务列表
   - 区分已完成（✅）、进行中（🔄）、待办（⏳）任务
   - 进行中的任务显示 active_form 描述

```python
@dataclass
class PlanItem: 
    id: str                     # 标记任务 id，便于辨识
    content: str                # 这一步要做什么
    status: str = "pending"     # pending | in_progress | completed
    active_form: str = ""       # 进行时描述

@dataclass
class PlanningState:
    items: list[PlanItem] = field(default_factory=list)
    rounds_since_update: int = 0

class TodoManager:
    def update(self, items: list) -> str:
        if len(items) > 20:
            return f"Error: Too many plan items ({len(items)}). Maximum allowed is 20."
        
        normalized = []
        in_progress_count = 0
        for index, raw_item in enumerate(items):
            id = str(raw_item.get("id", "")).strip()
            content = str(raw_item.get("content", "")).strip()
            status = str(raw_item.get("status", "pending")).lower()
            active_form = str(raw_item.get("activeForm", "")).strip()
            
            if not id:
                return f"Error: Item {index} missing 'id' field."
            if not content:
                return f"Error: Item {index} missing 'content' field."
            if status not in {"pending", "in_progress", "completed"}:
                return f"Error: Invalid status '{status}'."
            if status == "in_progress":
                in_progress_count += 1
            
            normalized.append(PlanItem(id=id, content=content, status=status, active_form=active_form))
        
        if in_progress_count > 1:
            return "Error: Only one plan item can be in_progress at a time."
        
        self.state.items = normalized
        self.state.rounds_since_update = 0
        return self.render()
    
    def render(self) -> str:
        if not self.state.items:
            return "(no plan items)"
        lines = ["## Plan"]
        for item in self.state.items:
            icon = {"pending": "⏳", "in_progress": "🔄", "completed": "✅"}[item.status]
            status_text = item.active_form if item.status == "in_progress" else item.content
            lines.append(f"- [{item.id}] {icon} {status_text}")
        return "\n".join(lines)
```

### 任务状态流转

```
pending ──────────────> in_progress ──────────────> completed
   │                         │                          │
   │                         │                          │
   └─────────────────────────┴──────────────────────────┘
                    (可以回退到任意状态)
```

### 与前一章的对比

| 特性 | Capiter 3 | Capiter 4 |
|------|-----------|-----------|
| 任务规划 | 无 | TodoManager |
| 状态追踪 | 无 | PlanItem.status |
| 进度提醒 | 无 | rounds_since_update |
| 可视化 | 无 | 状态图标渲染 |

### 架构图

```
┌──────────┐     ┌──────────────┐     ┌─────────────┐
│   LLM    │ ──> │ task_update  │ ──> │ TodoManager │
│  prompt  │     │   (tool)     │     │             │
└──────────┘     └──────────────┘     └──────┬──────┘
                                             │
                              ┌──────────────┼──────────────┐
                              ▼              ▼              ▼
                        ┌──────────┐  ┌──────────┐  ┌──────────┐
                        │ pending  │  │in_progress│  │completed │
                        │   ⏳     │  │    🔄     │  │    ✅     │
                        └──────────┘  └──────────┘  └──────────┘
```

### 任务列表示例

```markdown
## Plan
- [1] ⏳ 读取项目结构
- [2] ⏳ 分析核心模块
- [3] 🔄 正在编写测试用例
- [4] ✅ 完成文档更新
```

### 本章小结

Capiter 4 引入了任务管理系统，使 Agent 能够将复杂需求拆分为可执行的计划项。单任务进行中的约束确保任务执行的有序性，轮次追踪可用于触发计划审查。状态图标提供直观的进度可视化。

---

## Capiter 5: 子代理系统

**文件路径**: `code/v1_task_manager/chapter_5/s05_subagent.py`  
**完整分析**: `docs/zh/chapter_5/s05_subagent_文档.md`

### 核心组件

| 组件 | 类型 | 说明 |
|------|------|------|
| `run_subagent()` | function | 启动子代理执行子任务 |
| `PARENT_TOOLS` | dict | 主代理可用工具集（任务管理类） |
| `CHILD_TOOLS` | dict | 子代理可用工具集（执行类） |
| `task` | tool | 主代理委派任务给子代理 |
| `CHILD_SYSTEM` | str | 子代理系统提示词 |

### 功能改动详情

1. **主副代理工具分离**
   - 主代理：`read_file`、`load_skill`、`task_*` 系列工具
   - 子代理：`bash`、`write_file`、`edit_file` 执行工具
   - 工具分离确保主代理保持简洁上下文

2. **子代理函数**
   - `run_subagent()` 创建独立对话上下文执行子任务
   - 子代理完成后返回摘要给主代理
   - 支持嵌套调用（但需注意上下文限制）

3. **任务委派工具**
   - 新增 `task(prompt)` 工具供主代理委派任务
   - 主代理保持上下文简洁，仅看到命令与结果
   - 执行细节不会污染主代理上下文

4. **独立上下文管理**
   - 子代理拥有独立的 messages 历史
   - 执行细节不会污染主代理上下文
   - 子代理可以有独立的 system prompt

5. **工具集配置化**
   - `PARENT_TOOLS` 和 `CHILD_TOOLS` 字典定义工具权限
   - 便于调整主副代理的能力边界
   - 支持不同场景的工具集配置

```python
# 主代理工具集（任务管理类）
PARENT_TOOLS = {
    "read_file": {"type": "function", "function": {...}},
    "load_skill": {"type": "function", "function": {...}},
    "task_create": {"type": "function", "function": {...}},
    "task_update": {"type": "function", "function": {...}},
    "task_execute_ready": {"type": "function", "function": {...}}
}

# 子代理工具集（执行类）
CHILD_TOOLS = {
    "bash": {"type": "function", "function": {...}},
    "write_file": {"type": "function", "function": {...}},
    "edit_file": {"type": "function", "function": {...}}
}

def run_subagent(prompt: str) -> str:
    # 创建子代理独立对话
    child_history = [
        {"role": "system", "content": CHILD_SYSTEM},
        {"role": "user", "content": prompt}
    ]
    
    # 执行子代理循环
    child_state = LoopState(messages=child_history)
    while run_one_turn(child_state, tools=CHILD_TOOLS):
        pass
    
    # 提取摘要返回给主代理
    return extract_summary(child_state.messages)
```

### 主副代理职责划分

| 职责 | 主代理 | 子代理 |
|------|--------|--------|
| 任务规划 | ✅ | ❌ |
| 进度跟踪 | ✅ | ❌ |
| 文件读取 | ✅ | ❌ |
| 技能加载 | ✅ | ❌ |
| Shell 执行 | ❌ | ✅ |
| 文件写入 | ❌ | ✅ |
| 文件编辑 | ❌ | ✅ |

### 与前一章的对比

| 特性 | Capiter 4 | Capiter 5 |
|------|-----------|-----------|
| 执行模型 | 单代理 | 主代理 + 子代理 |
| 工具集 | 统一 | 主副分离 |
| 上下文 | 共享 | 独立子上下文 |
| 任务委派 | 无 | task 工具 |

### 架构图

```
┌───────────────┐
│  Main Agent   │
│  (planning)   │
│ ┌───────────┐ │
│ │ read_file │ │
│ │load_skill │ │
│ │ task_*    │ │
│ └───────────┘ │
└───────┬───────┘
        │ task(prompt)
        ▼
┌───────────────┐
│ Sub Agent     │
│ (execution)   │
│ ┌───────────┐ │
│ │  bash     │ │
│ │  write    │ │
│ │  edit     │ │
│ └───────────┘ │
└───────┬───────┘
        │ result summary
        ▼
┌───────────────┐
│  Main Agent   │
│  (continue)   │
└───────────────┘
```

### 子代理执行流程

```
1. 主代理调用 task(prompt="执行具体任务")
2. 创建子代理独立上下文：
   - system prompt: CHILD_SYSTEM
   - user prompt: 传入的 prompt
3. 执行子代理循环：
   - 使用 CHILD_TOOLS
   - 持续直到子代理完成
4. 提取执行摘要
5. 返回摘要给主代理
6. 主代理继续规划
```

### 本章小结

Capiter 5 引入子代理系统，将任务规划与执行分离。主代理负责任务拆分和进度跟踪，子代理负责具体执行。工具集分离确保主代理保持简洁的上下文，只关注高层决策。这种设计减少了主上下文的污染，提高了长会话的可管理性。

---

## Capiter 6: 上下文管理

**文件路径**: `code/v1_task_manager/chapter_6/s06_context.py`  
**完整分析**: `docs/zh/chapter_6/s06_context_文档.md`

### 核心组件

| 组件 | 类型 | 说明 |
|------|------|------|
| `CONTEXT_LIMIT` | const | 上下文长度上限（字符数） |
| `PERSIST_THRESHOLD` | const | 输出持久化阈值 |
| `TOOL_RESULTS_DIR` | Path | 工具结果存储目录 |
| `compact()` | function | 上下文压缩（LLM 摘要） |
| `estimate_tokens()` | function | 估算 token 数量 |

### 功能改动详情

1. **上下文预算管理**
   - `CONTEXT_LIMIT = 50000` 字符作为紧凑触发点
   - 超出限制时自动触发摘要压缩
   - 防止超出模型上下文窗口

2. **大输出持久化**
   - 工具输出超过 `PERSIST_THRESHOLD = 30000` 字符时写入文件
   - 上下文中仅保留预览和文件路径引用
   - 减少上下文占用同时保留完整输出

3. **工具结果目录**
   - `TOOL_RESULTS_DIR = .task_outputs/tool-results/`
   - 每个工具输出保存为独立文件
   - 文件命名包含时间戳和工具信息

4. **LLM 摘要压缩**
   - `auto_compact()` 调用 LLM 生成对话摘要
   - 替换原始历史为摘要继续对话
   - 保留关键决策和状态信息

5. **最近结果保留**
   - `KEEP_RECENT_TOOL_RESULTS = 3` 保留最近 N 条完整结果
   - 较早的结果被压缩或持久化
   - 平衡上下文完整性和长度

```python
CONTEXT_LIMIT = 50000
PERSIST_THRESHOLD = 30000
KEEP_RECENT_TOOL_RESULTS = 3

def estimate_tokens(messages: list) -> int:
    """Rough token estimate: ~4 chars per token."""
    return len(json.dumps(messages, default=str)) // 4

def auto_compact(messages: list) -> list:
    """Compress conversation history into a short continuation summary."""
    conversation_text = json.dumps(messages, default=str)[:80000]
    prompt = (
        "Summarize this conversation for continuity. Include:\n"
        "1) Task overview and success criteria\n"
        "2) Current state: completed work, files touched\n"
        "3) Key decisions and failed approaches\n"
        "4) Remaining next steps\n"
        "Be concise but preserve critical details.\n\n"
        + conversation_text
    )
    try:
        response = client.chat.completions.create(model=MODEL, messages=[{"role": "user", "content": prompt}])
        summary = response.choices[0].message.content.strip()
    except Exception as e:
        summary = f"(compact failed: {e})"
    
    continuation = (
        "This session continues from a previous conversation that was compacted. "
        f"Summary of prior context:\n\n{summary}\n\n"
        "Continue from where we left off without re-asking the user."
    )
    return [{"role": "user", "content": continuation}]
```

### 持久化输出格式

```python
def persist_tool_output(tool_name: str, output: str) -> str:
    """Save large output to file and return reference."""
    TOOL_RESULTS_DIR.mkdir(parents=True, exist_ok=True)
    timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
    filename = f"{tool_name}_{timestamp}.txt"
    filepath = TOOL_RESULTS_DIR / filename
    filepath.write_text(output)
    
    relative_path = filepath.relative_to(WORKDIR)
    preview = output[:PREVIEW_CHARS] + "..." if len(output) > PREVIEW_CHARS else output
    
    return f"(Output persisted to {relative_path})\n\nPreview:\n{preview}"
```

### 与前一章的对比

| 特性 | Capiter 5 | Capiter 6 |
|------|-----------|-----------|
| 上下文限制 | 无处理 | 自动 compact |
| 大输出处理 | 直接注入 | 持久化到文件 |
| 历史管理 | 累积 | 压缩 + 保留最近 |
| Token 估算 | 无 | estimate_tokens() |

### 架构图

```
┌─────────────┐
│ LLM output  │
│  >30k chars │
└──────┬──────┘
       │
       ▼
┌─────────────┐     ┌──────────────────┐
│  Persist to │ ──> │ .task_outputs/   │
│   file      │     │ tool-results/    │
└─────────────┘     └──────────────────┘
       │
       ▼
┌─────────────┐
│ Inject ref  │
│ + preview   │
│ into ctx    │
└─────────────┘

当上下文 > 50000 chars:
┌─────────────┐
│  Context    │
│  > LIMIT    │
└──────┬──────┘
       │
       ▼
┌─────────────┐     ┌─────────────┐
│ auto_compact│ ──> │    LLM      │
│             │     │  summarize  │
└──────┬──────┘     └─────────────┘
       │
       ▼
┌─────────────┐
│  Replace    │
│  history    │
│  with       │
│  summary    │
└─────────────┘
```

### 上下文管理策略

```
1. 每次工具执行后检查输出大小
2. 如果输出 > PERSIST_THRESHOLD:
   - 保存到文件
   - 返回引用 + 预览
3. 每轮对话后估算上下文大小
4. 如果上下文 > CONTEXT_LIMIT:
   - 调用 auto_compact()
   - 用摘要替换历史
5. 保留最近 KEEP_RECENT_TOOL_RESULTS 条完整结果
```

### 本章小结

Capiter 6 实现了上下文管理机制，通过输出持久化和 LLM 摘要压缩控制上下文长度。大输出自动保存到磁盘，历史对话可被压缩为摘要，确保长会话不会超出模型限制。Token 估算提供近似的上下文使用情况。

---

## Capiter 7: 权限系统

**文件路径**: `code/v1_task_manager/chapter_7/s07_permission_system.py`  
**完整分析**: `docs/zh/chapter_7/s07_permission_文档.md`

### 核心组件

| 组件 | 类型 | 说明 |
|------|------|------|
| `BashSecurityValidator` | class | Bash 命令安全验证 |
| `PermissionManager` | class | 权限决策管理器 |
| `MODES` | tuple | 权限模式（default/plan/auto） |
| `DEFAULT_RULES` | list | 默认权限规则列表 |
| `is_workspace_trusted()` | function | 工作区信任检查 |

### 功能改动详情

1. **Bash 安全验证**
   - `BashSecurityValidator` 检测危险命令模式
   - 验证规则：`sudo`、`rm -rf`、`$()`、`IFS=` 等
   - 严重模式（sudo、rm_rf）直接拒绝

2. **权限决策管线**
   - 四阶段：deny_rules → mode_check → allow_rules → ask_user
   - 首个匹配规则决定行为（allow/deny/ask）
   - 流水线设计便于扩展新规则

3. **三种权限模式**
   - `default`：非只读工具需用户确认
   - `plan`：更严格的确认策略
   - `auto`：仅高风险操作需确认

4. **规则匹配系统**
   - 规则格式：`{tool, path/content, behavior}`
   - 支持通配符 `*` 匹配
   - 规则按顺序检查，首个匹配生效

5. **连续拒绝追踪**
   - `consecutive_denials` 计数器防止无限询问
   - 达到 `max_consecutive_denials` 后自动停止
   - 避免模型陷入询问循环

```python
class BashSecurityValidator:
    VALIDATORS = [
        ("sudo", r"\bsudo\b"),
        ("rm_rf", r"\brm\s+(-[a-zA-Z]*)?r"),
        ("cmd_substitution", r"\$\("),
        ("ifs_injection", r"\bIFS\s*="),
    ]
    
    def validate(self, command: str) -> list:
        failures = []
        for name, pattern in self.VALIDATORS:
            if re.search(pattern, command):
                failures.append((name, pattern))
        return failures
    
    def is_safe(self, command: str) -> bool:
        return len(self.validate(command)) == 0

class PermissionManager:
    def __init__(self, mode: str = "default", rules: list = None):
        if mode not in MODES:
            raise ValueError(f"Unknown mode: {mode}")
        self.mode = mode
        self.rules = rules or list(DEFAULT_RULES)
        self.consecutive_denials = 0
        self.max_consecutive_denials = 3
    
    def check(self, tool_name: str, tool_input: dict) -> dict:
        # Step 0: Bash security validation
        if tool_name == "bash":
            command = tool_input.get("command", "")
            failures = bash_validator.validate(command)
            if failures:
                severe = {"sudo", "rm_rf"}
                if any(f[0] in severe for f in failures):
                    return {"behavior": "deny", "reason": bash_validator.describe_failures(command)}
        
        # Step 1-3: Rule-based permission check
        for rule in self.rules:
            if self._matches_rule(rule, tool_name, tool_input):
                return {"behavior": rule["behavior"], "reason": f"Matched rule: {rule}"}
        
        # Default behavior based on mode
        if tool_name in READ_ONLY_TOOLS:
            return {"behavior": "allow", "reason": "Read-only tool"}
        return {"behavior": "ask", "reason": f"{tool_name} requires confirmation"}
```

### 权限规则示例

```python
DEFAULT_RULES = [
    # 总是拒绝危险命令
    {"tool": "bash", "content": "rm -rf /", "behavior": "deny"},
    {"tool": "bash", "content": "sudo *", "behavior": "deny"},
    # 允许读取任何文件
    {"tool": "read_file", "path": "*", "behavior": "allow"},
    # 允许写入特定目录
    {"tool": "write_file", "path": "logs/*", "behavior": "allow"},
]
```

### 与前一章的对比

| 特性 | Capiter 6 | Capiter 7 |
|------|-----------|-----------|
| 安全控制 | 简单黑名单 | 多级权限管线 |
| 用户交互 | 无 | ask 模式询问 |
| 规则系统 | 无 | 可配置规则列表 |
| 模式支持 | 无 | default/plan/auto |

### 架构图

```
┌─────────────┐
│ Tool Call   │
└──────┬──────┘
       │
       ▼
┌─────────────────────┐
│ BashSecurityValidator│
│ (check dangerous)   │
└──────┬──────────────┘
       │ safe
       ▼
┌─────────────────────┐
│  PermissionManager  │
│  1. deny rules      │
│  2. mode check      │
│  3. allow rules     │
│  4. ask user        │
└──────┬──────────────┘
       │
   ┌───┴───┐
   ▼       ▼
 allow   deny/ask
```

### 权限决策流程

```
1. 工具调用发生
2. BashSecurityValidator 检查危险命令
   - 如果严重违规：直接 deny
3. PermissionManager 检查规则：
   a. deny_rules：匹配则 deny
   b. mode_check：根据模式决定
   c. allow_rules：匹配则 allow
   d. 默认：ask 用户
4. 返回决策给调用方
```

### 本章小结

Capiter 7 建立了完整的权限系统，包含 Bash 命令安全验证和多级权限决策管线。三种模式适应不同场景，规则系统支持灵活配置，连续拒绝追踪防止无限询问。权限系统与工具调用深度集成，确保操作安全。

---

## Capiter 8: Hook 系统

**文件路径**: `code/v1_task_manager/chapter_8/s08_hook_system.py`  
**完整分析**: `docs/zh/chapter_8/s08_hook_文档.md`

### 核心组件

| 组件 | 类型 | 说明 |
|------|------|------|
| `HookManager` | class | Hook 管理器，加载和执行钩子 |
| `HOOK_EVENTS` | tuple | 支持的事件类型（PreToolUse/PostToolUse/SessionStart） |
| `.hooks.json` | file | Hook 配置文件，定义外部脚本 |
| `run_pre_tool_use()` | method | 工具调用前拦截，执行权限检查和外部 Hook |
| `run_post_tool_use()` | method | 工具调用后拦截，执行外部 Hook |
| `_run_external_hooks()` | method | 执行外部脚本 Hook |

### 功能改动详情

1. **Hook 事件类型**
   - `PreToolUse`：工具调用前触发，可用于权限审查、参数修改
   - `PostToolUse`：工具调用后触发，可用于结果处理、日志记录
   - `SessionStart`：会话开始时触发，可用于初始化

2. **统一拦截管线**
   - Ring 0：内置安全/权限检查（PermissionManager）
   - Ring 1：外部自定义 Hook 脚本
   - 两阶段处理确保安全优先

3. **配置文件格式**
   - `.hooks.json` 定义外部 Hook 命令
   - 支持 matcher 指定触发工具（`*` 表示所有工具）
   - 命令通过 subprocess 执行

4. **环境变量注入**
   - Hook 执行时注入 `HOOK_EVENT`、`HOOK_TOOL_NAME` 等环境变量
   - 脚本可访问工具输入输出
   - 支持脚本修改工具输入（updated_input）

5. **阻塞能力**
   - Hook 可返回 `{"blocked": true, "block_reason": "..."}` 阻止工具执行
   - 支持修改工具输入（updated_input）
   - 支持添加额外消息到上下文

```python
HOOK_EVENTS = ("PreToolUse", "PostToolUse", "SessionStart")
HOOK_TIMEOUT = 30  # seconds

class HookManager:
    def __init__(self, perms_manager, config_path: Path = None, sdk_mode: bool = True):
        self.perms = perms_manager  # 注入权限管理器
        self.hooks = {"PreToolUse": [], "PostToolUse": [], "SessionStart": []}
        self._sdk_mode = sdk_mode
        config_path = config_path or (WORKDIR / ".hooks.json")
        if config_path.exists():
            try:
                config = json.loads(config_path.read_text())
                for event in HOOK_EVENTS:
                    self.hooks[event] = config.get("hooks", {}).get(event, [])
                print(f"[Hooks loaded from {config_path}]")
            except Exception as e:
                print(f"[Hook config error: {e}]")
    
    def run_pre_tool_use(self, context: dict) -> dict:
        """统一的 PreToolUse 拦截管线。"""
        result = {"blocked": False, "block_reason": "", "messages": []}
        tool_name = context.get("tool_name", "")
        tool_input = context.get("tool_input", {})

        # Ring 0: 内置安全与权限 Hook
        decision = self.perms.check(tool_name, tool_input)
        if decision["behavior"] == "deny":
            return {"blocked": True, "block_reason": f"Permission denied: {decision['reason']}"}
        elif decision["behavior"] == "ask":
            if not self.perms.ask_user(tool_name, tool_input):
                return {"blocked": True, "block_reason": f"User denied execution for {tool_name}"}

        # Ring 1: 外部自定义 Hook
        ext_result = self._run_external_hooks("PreToolUse", context)
        if ext_result["blocked"]:
            return ext_result
        if "updated_input" in ext_result:
            context["tool_input"] = ext_result["updated_input"]
        return result
    
    def _run_external_hooks(self, event: str, context: dict) -> dict:
        result = {"blocked": False, "block_reason": "", "messages": []}
        hooks = self.hooks.get(event, [])
        for hook_def in hooks:
            matcher = hook_def.get("matcher")
            if matcher and context:
                tool_name = context.get("tool_name", "")
                if matcher != "*" and matcher != tool_name:
                    continue
            
            command = hook_def.get("command", "")
            if not command: continue
            
            env = dict(os.environ)
            env["HOOK_EVENT"] = event
            env["HOOK_TOOL_NAME"] = context.get("tool_name", "")
            env["HOOK_TOOL_INPUT"] = json.dumps(context.get("tool_input", {}))[:10000]
            
            r = subprocess.run(command, shell=True, cwd=WORKDIR, env=env,
                             capture_output=True, text=True, timeout=HOOK_TIMEOUT)
            if r.returncode != 0:
                result["messages"].append(f"Hook error: {r.stderr}")
        return result
```

### Hook 配置示例

```json
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "bash",
        "command": "echo 'Executing bash: $HOOK_TOOL_INPUT' >> .hook_log.txt"
      },
      {
        "matcher": "*",
        "command": "python scripts/audit_tool.py"
      }
    ],
    "PostToolUse": [
      {
        "matcher": "write_file",
        "command": "git add $HOOK_TOOL_INPUT_PATH"
      }
    ]
  }
}
```

### 与前一章的对比

| 特性 | Capiter 7 | Capiter 8 |
|------|-----------|-----------|
| 权限集成 | 直接调用 | Hook 管线 Ring 0 |
| 扩展点 | 无 | 外部脚本 Hook |
| 配置方式 | 代码 | .hooks.json |
| 事件类型 | 无 | 3 种 Hook 事件 |

### 架构图

```
┌─────────────┐
│ PreToolUse  │
└──────┬──────┘
       │
       ▼
┌─────────────────────┐
│  Ring 0: Internal   │
│  PermissionManager  │
└──────┬──────────────┘
       │ passed
       ▼
┌─────────────────────┐
│  Ring 1: External   │
│  .hooks.json hooks  │
└──────┬──────────────┘
       │
   ┌───┴───┐
   ▼       ▼
 execute  blocked
```

### Hook 执行流程

```
1. 工具调用触发 PreToolUse 事件
2. HookManager.run_pre_tool_use() 执行：
   a. Ring 0: PermissionManager 检查权限
      - deny: 直接返回 blocked
      - ask: 询问用户，拒绝则 blocked
      - allow: 继续
   b. Ring 1: 外部 Hook 脚本
      - 执行匹配的命令
      - 可修改输入或返回 blocked
3. 如果未 blocked，执行工具
4. 工具完成后触发 PostToolUse 事件
5. 执行 PostToolUse Hook
```

### 本章小结

Capiter 8 将权限审查集成到 Hook 系统中，形成统一的拦截管线。Ring 0 处理内置安全检查，Ring 1 支持外部脚本扩展。Hook 系统为框架提供了可插拔的事件处理能力，支持工具调用前后自定义逻辑。

---

## Capiter 9: 记忆系统

**文件路径**: `code/v1_task_manager/chapter_9/s09_memory_system.py`  
**完整分析**: `docs/zh/chapter_9/s09_memory_system_文档.md`

### 核心组件

| 组件 | 类型 | 说明 |
|------|------|------|
| `MemoryManager` | class | 记忆管理器，加载和保存记忆 |
| `MEMORY_TYPES` | tuple | 记忆类型（user/feedback/project/reference） |
| `MEMORY_DIR` | Path | 记忆存储目录（.memory/） |
| `MEMORY_INDEX` | Path | 记忆索引文件（MEMORY.md） |
| `load_memory_prompt()` | method | 构建记忆提示词注入到 system prompt |

### 功能改动详情

1. **记忆数据结构**
   - 每条记忆为独立 Markdown 文件
   - frontmatter 包含 name、description、type
   - 内容 body 存储具体记忆信息

2. **四种记忆类型**
   - `user`：用户偏好和习惯（通常 private）
   - `feedback`：用户反馈（默认 private）
   - `project`：项目特定知识（通常 team）
   - `reference`：参考资料（通常 team）

3. **记忆索引**
   - `MEMORY.md` 作为记忆索引文件
   - 限制最多 200 行防止过长
   - 便于快速浏览所有记忆

4. **系统提示注入**
   - `load_memory_prompt()` 在会话启动时加载记忆
   - 按类型分组展示到 system prompt
   - 记忆内容在会话中持久可用

5. **记忆存储原则**
   - 仅存储跨会话有效的知识
   - 不存储可重新推导的信息（文件结构、临时状态等）
   - 避免存储敏感信息（密钥、密码等）

```python
MEMORY_TYPES = ("user", "feedback", "project", "reference")
MEMORY_DIR = WORKDIR / ".memory"
MEMORY_INDEX = MEMORY_DIR / "MEMORY.md"
MAX_INDEX_LINES = 200

class MemoryManager:
    def __init__(self, memory_dir: Path = None):
        self.memory_dir = memory_dir or MEMORY_DIR
        self.memories = {}  # name -> {description, type, content}
    
    def load_all(self):
        """Load MEMORY.md index and all individual memory files."""
        self.memories = {}
        if not self.memory_dir.exists():
            return
        for md_file in sorted(self.memory_dir.glob("*.md")):
            if md_file.name == "MEMORY.md":
                continue
            parsed = self._parse_frontmatter(md_file.read_text())
            if parsed:
                name = parsed.get("name", md_file.stem)
                self.memories[name] = {
                    "description": parsed.get("description", ""),
                    "type": parsed.get("type", "project"),
                    "content": parsed.get("content", ""),
                    "file": md_file.name,
                }
    
    def load_memory_prompt(self) -> str:
        """Build a memory section for injection into the system prompt."""
        if not self.memories:
            return ""
        sections = ["# Memories (persistent across sessions)", ""]
        for mem_type in MEMORY_TYPES:
            typed = {k: v for k, v in self.memories.items() if v["type"] == mem_type}
            if not typed:
                continue
            sections.append(f"## [{mem_type}]")
            for name, mem in typed.items():
                sections.append(f"### {name}: {mem['description']}")
                if mem["content"].strip():
                    sections.append(mem["content"].strip())
                sections.append("")
        return "\n".join(sections)
    
    def save_memory(self, name: str, description: str, mem_type: str, content: str) -> str:
        if mem_type not in MEMORY_TYPES:
            return f"Error: type must be one of {MEMORY_TYPES}"
        safe_name = re.sub(r"[^a-zA-Z0-9_-]", "_", name.lower())
        file_name = f"{safe_name}.md"
        file_path = self.memory_dir / file_name
        
        frontmatter = f"---\nname: {name}\ndescription: {description}\ntype: {mem_type}\n---\n{content}\n"
        file_path.write_text(frontmatter)
        return f"Memory '{name}' saved to {file_name}"
```

### 记忆文件示例

```markdown
---
name: user_coding_style
description: 用户偏好使用 snake_case 命名和详细注释
type: user
---

用户在编写 Python 代码时偏好：
- 使用 snake_case 命名变量和函数
- 添加详细的 docstring
- 类型注解使用 typing 模块
- 错误处理使用 try-except 显式捕获
```

### 与前一章的对比

| 特性 | Capiter 8 | Capiter 9 |
|------|-----------|-----------|
| 持久化知识 | 无 | MemoryManager |
| 类型区分 | 无 | 4 种记忆类型 |
| 会话继承 | 无 | 跨会话记忆 |
| 索引文件 | 无 | MEMORY.md |

### 架构图

```
┌───────────────────┐
│ .memory/          │
│  user_pref.md     │
│  project_info.md  │
│  MEMORY.md        │
└─────────┬─────────┘
          │
          ▼
┌───────────────────┐
│  MemoryManager    │
│  load_all()       │
└─────────┬─────────┘
          │
          ▼
┌───────────────────┐
│  System Prompt    │
│  + Memories       │
│    (grouped by    │
│     type)         │
└──────────────────┘
```

### 记忆管理流程

```
1. 会话启动时：
   - MemoryManager.load_all() 扫描 .memory/ 目录
   - 解析每个 .md 文件的 frontmatter
   - 建立内存索引
2. 构建 system prompt:
   - load_memory_prompt() 按类型分组
   - 注入到 system prompt
3. 会话中：
   - 模型可查看记忆内容
   - 可根据记忆调整行为
4. 需要保存记忆时：
   - save_memory() 创建/更新 .md 文件
   - 下次会话自动加载
```

### 本章小结

Capiter 9 实现了跨会话记忆系统，支持用户偏好、项目知识等持久化存储。记忆按类型分组，仅存储无法从当前工作重新推导的知识，避免冗余和过时信息。frontmatter 格式支持结构化的元数据管理。

---

## Capiter 10: 构建系统

**文件路径**: `code/v1_task_manager/chapter_10/s10_build_system.py`  
**完整分析**: `docs/zh/chapter_10/s10_build_system_文档.md`

### 核心组件

| 组件 | 类型 | 说明 |
|------|------|------|
| `SystemPromptBuilder` | class | 系统提示构建器 |
| `build()` | method | 组装完整 system prompt |
| `_build_core()` | method | 构建核心指令部分 |
| `_build_memory()` | method | 注入记忆部分 |
| `_build_claude_md_chain()` | method | 读取 CLAUDE.md 文档链 |
| `_build_dynamic_context()` | method | 注入动态上下文 |

### 功能改动详情

1. **结构化提示词**
   - 固定组：核心指令、工具列表、技能元数据
   - 动态组：记忆、CLAUDE.md 链、动态上下文
   - 模块化设计便于维护和扩展

2. **系统提示构建器**
   - `SystemPromptBuilder` 类负责组装各部分
   - 支持主代理和子代理不同配置
   - 按需注入不同组件

3. **Memory 集成**
   - `load_memory_prompt()` 注入持久化记忆
   - 按类型分组展示
   - 仅在记忆存在时注入

4. **项目文档链**
   - 读取 `CLAUDE.md` 及其引用链
   - 注入项目特定规范到提示词
   - 支持相对路径和文件不存在处理

5. **动态上下文**
   - 根据当前状态注入额外信息
   - 如任务列表、worktree 状态等
   - 保持提示词与当前状态同步

```python
class SystemPromptBuilder:
    def __init__(self, agent_type: str = "parent"):
        self.agent_type = agent_type
        self.tools = PARENT_TOOLS if agent_type == "parent" else CHILD_TOOLS
    
    def _build_core(self) -> str:
        return f"""You are a coding agent operating at {WORKDIR}.

## Core Principles
1. Use tools to complete tasks efficiently
2. Follow project guidelines and user preferences
3. Maintain context within limits

## Available Tools
{self._list_tools()}

## Skills Available
{self._list_skills()}
"""
    
    def _build_memory(self) -> str:
        return self.memory_manager.load_memory_prompt()
    
    def _build_claude_md_chain(self) -> str:
        parts = []
        claude_md = WORKDIR / "CLAUDE.md"
        if claude_md.exists():
            parts.append("## Project Guidelines (CLAUDE.md)")
            parts.append(claude_md.read_text()[:10000])
        return "\n\n".join(parts)
    
    def _build_dynamic_context(self) -> str:
        parts = ["## Dynamic Context"]
        # 添加任务状态
        if self.task_manager:
            parts.append(self.task_manager.list_all())
        # 添加 worktree 状态
        if self.worktree_manager:
            parts.append(self.worktree_manager.list())
        return "\n\n".join(parts) if len(parts) > 1 else ""
    
    def build(self) -> str:
        parts = [
            self._build_core(),
            self._build_memory(),
            self._build_claude_md_chain(),
            self._build_dynamic_context(),
        ]
        return "\n\n".join(p for p in parts if p)
```

### 提示词结构

```
# System Prompt

## Core Instructions (固定)
- Agent 角色定义
- 核心原则
- 可用工具列表
- 可用技能列表

## Memories (动态)
- [user] 用户偏好
- [project] 项目知识
- ...

## Project Guidelines (动态)
- CLAUDE.md 内容

## Dynamic Context (动态)
- 当前任务列表
- Worktree 状态
- 其他运行时信息
```

### 与前一章的对比

| 特性 | Capiter 9 | Capiter 10 |
|------|-----------|------------|
| 提示词组织 | 拼接 | 结构化构建器 |
| 文档链 | 无 | CLAUDE.md 链 |
| 动态注入 | 无 | 动态上下文 |
| 主副区分 | 无 | agent_type 参数 |

### 架构图

```
┌─────────────────────┐
│ SystemPromptBuilder │
└──────────┬──────────┘
           │
    ┌──────┼──────┬──────────┬──────────────┐
    ▼      ▼      ▼          ▼              ▼
┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐  ┌────────────┐
│ Core │ │Memory│ │Skills│ │Claude│  │  Dynamic   │
│      │ │      │ │      │ │  MD  │  │  Context   │
└──────┘ └──────┘ └──────┘ └──────┘  └────────────┘
    │      │      │          │              │
    └──────┴──────┴──────────┴──────────────┘
                           │
                           ▼
                  ┌────────────────┐
                  │ System Prompt  │
                  └────────────────┘
```

### 本章小结

Capiter 10 实现了结构化的系统提示构建器，将提示词分为固定组和动态组。Memory、Skills、CLAUDE.md 链等组件按需注入，支持主副代理不同配置，便于维护和扩展。动态上下文确保提示词与运行时状态同步。

---

## Capiter 11: 恢复系统

**文件路径**: `code/v1_task_manager/chapter_11/s11_Resume_system.py`  
**完整分析**: `docs/zh/chapter_11/s11_Resume_system_文档.md`

### 核心组件

| 组件 | 类型 | 说明 |
|------|------|------|
| `MAX_RECOVERY_ATTEMPTS` | const | 最大恢复尝试次数（3 次） |
| `BACKOFF_BASE_DELAY` | const | 指数退避基础延迟（1 秒） |
| `auto_compact()` | function | 自动上下文压缩 |
| `backoff_delay()` | function | 计算退避延迟 |
| 多层错误捕获 | concept | 用户输入/API/工具错误处理 |

### 功能改动详情

1. **多层错误处理**
   - 用户输入错误（EOF、键盘中断）
   - 主循环错误（API 调用、工具执行）
   - 子循环错误（子代理执行）
   - 每层都有相应的恢复策略

2. **输出超限恢复**
   - 检测 `max_tokens` 停止原因
   - 注入继续消息："Output limit hit. Continue directly."
   - 最多重试 `MAX_RECOVERY_ATTEMPTS = 3` 次
   - 计数器防止无限重试

3. **API 错误恢复**
   - `prompt_too_long`：触发 auto_compact + 重试
   - `connection/rate`：指数退避重试
   - 其他错误：返回错误信息

4. **指数退避策略**
   - `backoff_delay()`: base * 2^attempt + jitter
   - 最大延迟 30 秒，避免频繁重试
   - 随机抖动防止并发冲突

5. **上下文压缩触发**
   - 估算 token 数：`len(json.dumps(messages)) // 4`
   - 超限时调用 LLM 生成摘要
   - 摘要保留关键信息

```python
MAX_RECOVERY_ATTEMPTS = 3
BACKOFF_BASE_DELAY = 1.0
BACKOFF_MAX_DELAY = 30.0

CONTINUATION_MESSAGE = (
    "Output limit hit. Continue directly from where you stopped -- "
    "no recap, no repetition. Pick up mid-sentence if needed."
)

def backoff_delay(attempt: int) -> float:
    """Exponential backoff with jitter: base * 2^attempt + random(0, 1)."""
    delay = min(BACKOFF_BASE_DELAY * (2 ** attempt), BACKOFF_MAX_DELAY)
    jitter = random.uniform(0, 1)
    return delay + jitter

def auto_compact(messages: list) -> list:
    """Compress conversation history into a short continuation summary."""
    conversation_text = json.dumps(messages, default=str)[:80000]
    prompt = (
        "Summarize this conversation for continuity. Include:\n"
        "1) Task overview and success criteria\n"
        "2) Current state: completed work, files touched\n"
        "3) Key decisions and failed approaches\n"
        "4) Remaining next steps\n"
        "Be concise but preserve critical details.\n\n"
        + conversation_text
    )
    try:
        response = client.chat.completions.create(model=MODEL, messages=[{"role": "user", "content": prompt}])
        summary = response.choices[0].message.content.strip()
    except Exception as e:
        summary = f"(compact failed: {e})"
    
    continuation = (
        "This session continues from a previous conversation that was compacted. "
        f"Summary of prior context:\n\n{summary}\n\n"
        "Continue from where we left off without re-asking the user."
    )
    return [{"role": "user", "content": continuation}]

# 主循环中的错误处理
def run_one_turn(state: LoopState) -> bool:
    for attempt in range(MAX_RECOVERY_ATTEMPTS):
        try:
            response = client.chat.completions.create(...)
            # 检查停止原因
            if response.choices[0].finish_reason == "max_tokens":
                state.messages.append({"role": "user", "content": CONTINUATION_MESSAGE})
                continue  # 重试
            # 正常处理...
            return True
        except APIError as e:
            if e.type == "prompt_too_long":
                state.messages = auto_compact(state.messages)
                continue
            elif e.type in ("connection", "rate_limit"):
                time.sleep(backoff_delay(attempt))
                continue
            raise
    return False
```

### 错误类型与恢复策略

| 错误类型 | 检测方式 | 恢复策略 |
|----------|----------|----------|
| max_tokens | finish_reason | 注入继续消息 + 重试 |
| prompt_too_long | API error type | auto_compact + 重试 |
| connection | API error type | 指数退避 + 重试 |
| rate_limit | API error type | 指数退避 + 重试 |
| 其他 | - | 抛出异常 |

### 与前一章的对比

| 特性 | Capiter 10 | Capiter 11 |
|------|------------|------------|
| 错误处理 | 基础 | 多层恢复策略 |
| 输出超限 | 无 | 继续注入恢复 |
| API 重试 | 无 | 指数退避 |
| 压缩触发 | 无 | prompt_too_long |

### 架构图

```
┌─────────────┐
│ LLM Response│
└──────┬──────┘
       │
       ▼
┌─────────────────────┐
│ Check stop_reason   │
└──────┬──────────────┘
       │
   ┌───┴───────────────┬─────────────┐
   ▼                   ▼             ▼
max_tokens          API error     end_turn
   │                   │             │
   ▼                   ▼             │
Continue inject    ┌──┴──────┐      │
   │               ▼         ▼      │
   │         prompt_too   conn/rate │
   │           long       │         │
   │           │          │         │
   │           ▼          ▼         │
   │       compact    backoff       │
   │           │          │         │
   └───────────┴──────────┴─────────┘
                       │
                       ▼
                  Retry loop
                  (max 3 times)
```

### 恢复流程示例

```
1. LLM 返回 finish_reason="max_tokens"
2. 检测到输出超限
3. 注入 CONTINUATION_MESSAGE
4. 重试 LLM 调用
5. 如果成功，继续正常流程
6. 如果仍超限，计数器 +1
7. 达到 MAX_RECOVERY_ATTEMPTS 后放弃

API 错误流程：
1. 捕获 APIError
2. 检查错误类型
3. prompt_too_long → auto_compact → 重试
4. connection/rate → backoff_delay → 重试
5. 其他错误 → 抛出
```

### 本章小结

Capiter 11 增强了错误恢复能力，针对输出超限、API 错误、连接问题等不同场景实现差异化恢复策略。多层错误处理确保框架在异常情况下能够优雅降级并继续执行。指数退避和重试限制防止无限循环。

---

## Capiter 12: 任务系统增强

**文件路径**: `code/v1_task_manager/chapter_12/s12_task_system.py`  
**完整分析**: `docs/zh/chapter_12/s12_task_system_文档.md`

### 核心组件

| 组件 | 类型 | 说明 |
|------|------|------|
| `TaskManager` | class | 任务 CRUD 管理器 |
| `TASKS_DIR` | Path | 任务存储目录（.tasks/） |
| task_create/update/list/get | tools | 任务操作工具 |
| blockedBy/blocks | fields | 任务依赖关系字段 |
| `_clear_dependency()` | method | 完成任务时清除依赖 |

### 功能改动详情

1. **持久化任务存储**
   - 每个任务保存为 `task_{id}.json` 文件
   - 支持跨会话任务追踪
   - 文件路径：`.tasks/task_1.json`、`.tasks/task_2.json` 等

2. **任务依赖关系**
   - `blockedBy`：当前任务被哪些任务阻塞
   - `blocks`：当前任务阻塞哪些任务
   - 双向关联自动维护
   - 支持任务图结构

3. **任务状态管理**
   - 状态：`pending`、`in_progress`、`completed`、`deleted`
   - 完成任务时自动清除依赖
   - 状态变更触发依赖更新

4. **任务 CRUD 工具**
   - `task_create`：创建新任务
   - `task_update`：更新状态/依赖
   - `task_list`：列出所有任务
   - `task_get`：获取单个任务详情

5. **任务 ID 自增**
   - `_max_id()` 读取现有任务计算下一个 ID
   - 确保 ID 唯一性和连续性
   - 支持删除后 ID 复用（可选）

```python
class TaskManager:
    def __init__(self, tasks_dir: Path):
        self.dir = tasks_dir
        self.dir.mkdir(exist_ok=True)
        self._next_id = self._max_id() + 1
    
    def _max_id(self) -> int:
        ids = [int(f.stem.split("_")[1]) for f in self.dir.glob("task_*.json")]
        return max(ids) if ids else 0
    
    def _load(self, task_id: int) -> dict:
        path = self.dir / f"task_{task_id}.json"
        if not path.exists():
            raise ValueError(f"Task {task_id} not found")
        return json.loads(path.read_text())
    
    def _save(self, task: dict):
        path = self.dir / f"task_{task['id']}.json"
        path.write_text(json.dumps(task, indent=2, ensure_ascii=False))
    
    def create(self, subject: str, description: str = "") -> str:
        task = {
            "id": self._next_id, "subject": subject, "description": description,
            "status": "pending", "blockedBy": [], "blocks": [], "owner": "",
        }
        self._save(task)
        self._next_id += 1
        return json.dumps(task, indent=2, ensure_ascii=False)
    
    def update(self, task_id: int, status: str = None, owner: str = None,
               add_blocked_by: list = None, add_blocks: list = None) -> str:
        task = self._load(task_id)
        if status:
            task["status"] = status
            if status == "completed":
                self._clear_dependency(task_id)
        if add_blocked_by:
            task["blockedBy"] = list(set(task["blockedBy"] + add_blocked_by))
        if add_blocks:
            task["blocks"] = list(set(task["blocks"] + add_blocks))
            for blocked_id in add_blocks:
                try:
                    blocked = self._load(blocked_id)
                    if task_id not in blocked["blockedBy"]:
                        blocked["blockedBy"].append(task_id)
                        self._save(blocked)
                except ValueError:
                    pass
        self._save(task)
        return json.dumps(task, indent=2, ensure_ascii=False)
    
    def _clear_dependency(self, completed_id: int):
        """Remove completed_id from all other tasks' blockedBy lists."""
        for f in self.dir.glob("task_*.json"):
            task = json.loads(f.read_text())
            if completed_id in task.get("blockedBy", []):
                task["blockedBy"].remove(completed_id)
                self._save(task)
```

### 任务数据结构

```json
{
  "id": 1,
  "subject": "实现用户认证模块",
  "description": "添加登录、注册、token 验证功能",
  "status": "in_progress",
  "blockedBy": [],
  "blocks": [2, 3],
  "owner": "agent_1"
}
```

### 与前一章的对比

| 特性 | Capiter 11 | Capiter 12 |
|------|------------|------------|
| 任务管理 | TodoManager（内存） | TaskManager（持久化） |
| 依赖关系 | 无 | blockedBy/blocks |
| 跨会话 | 无 | 文件存储 |
| 任务图 | 无 | 双向依赖 |

### 架构图

```
┌─────────────┐
│ TaskManager │
└──────┬──────┘
       │
       ▼
┌─────────────────────┐
│ .tasks/             │
│  task_1.json        │
│  task_2.json        │
│  task_3.json        │
└─────────────────────┘

Task structure:
{
  "id": 1,
  "subject": "...",
  "status": "pending",
  "blockedBy": [2],  # 被 task_2 阻塞
  "blocks": [3],     # 阻塞 task_3
  "owner": ""
}

依赖关系示例:
task_1 (completed) ──blocks──> task_2 (pending)
                              │
                              └──blockedBy task_1 (自动清除)
```

### 任务依赖流程

```
1. 创建 task_1，blocks=[2]
2. 创建 task_2，blockedBy=[1]
   - 自动建立双向关联
3. 完成 task_1:
   - _clear_dependency(1) 遍历所有任务
   - 从 task_2.blockedBy 移除 1
4. task_2 不再被阻塞，可开始执行
```

### 本章小结

Capiter 12 将任务管理从内存升级到持久化存储，支持跨会话任务追踪。任务依赖关系系统（blockedBy/blocks）支持复杂任务图，完成任务时自动清理依赖关系。双向关联确保依赖状态一致性。

---

## Capiter 13: v2 后台任务

**文件路径**: `code/v1_task_manager/chapter_13/s13_v2_backtask.py`  
**完整分析**: `docs/zh/chapter_13/s13_v2_backtask_文档.md`

### 核心组件

| 组件 | 类型 | 说明 |
|------|------|------|
| `BackgroundManager` | class | 后台任务管理器 |
| `NotificationQueue` | class | 优先级通知队列 |
| `run_subagent_background()` | function | 后台子代理执行 |
| `check_background()` | function | 查询后台任务状态 |
| `detect_stalled()` | method | 检测停滞任务 |

### 功能改动详情

1. **后台子代理**
   - `run_subagent_background()` 在后台线程启动子代理
   - 立即返回 task_id（格式 `sub_xxxxxxxx`）
   - 完成后推送摘要到通知队列
   - 主代理可继续其他工作

2. **通知队列**
   - `NotificationQueue` 支持优先级（immediate/high/medium/low）
   - 同 key 消息折叠，避免重复通知
   - `drain()` 方法供主循环获取待处理通知
   - 线程安全的队列操作

3. **工具变更**
   - 新增：`background_task(prompt)`、`check_background(task_id?)`
   - 删除：`task(prompt)`（串行子代理）
   - 主代理工具集调整为异步模式

4. **任务状态追踪**
   - `RUNTIME_DIR/.runtime-tasks/` 存储任务记录
   - 状态：`running`、`completed`、`stalled`
   - 输出保存到独立日志文件
   - 支持任务查询和状态检查

5. **停滞检测**
   - `STALL_THRESHOLD_S = 45` 秒判定停滞
   - `detect_stalled()` 检查超时任务
   - 停滞任务可被清理或重新调度

```python
class NotificationQueue:
    PRIORITIES = {"immediate": 0, "high": 1, "medium": 2, "low": 3}
    def __init__(self):
        self._queue = []
        self._lock = threading.Lock()
    
    def push(self, message: str, priority: str = "medium", key: str = None):
        with self._lock:
            if key:
                self._queue = [(p, k, m) for p, k, m in self._queue if k != key]
            self._queue.append((self.PRIORITIES.get(priority, 2), key, message))
            self._queue.sort(key=lambda x: x[0])
    
    def drain(self) -> list:
        with self._lock:
            messages = [m for _, _, m in self._queue]
            self._queue.clear()
            return messages

class BackgroundManager:
    def __init__(self):
        self.dir = RUNTIME_DIR
        self.tasks = {}
        self._lock = threading.Lock()
    
    def run(self, command: str) -> str:
        task_id = str(uuid.uuid4())[:8]
        output_file = self.dir / f"{task_id}.log"
        
        with self._lock:
            self.tasks[task_id] = {
                "id": task_id, "status": "running", "command": command,
                "started_at": time.time(), "result": None
            }
        
        thread = threading.Thread(
            target=self._execute, args=(task_id, command, output_file), daemon=True
        )
        thread.start()
        return f"Background task started: {task_id}"
    
    def _execute(self, task_id: str, command: str, output_file: Path):
        try:
            result = subprocess.run(command, shell=True, capture_output=True, text=True)
            with self._lock:
                self.tasks[task_id]["status"] = "completed"
                self.tasks[task_id]["result"] = result.stdout[:500]
                self.tasks[task_id]["finished_at"] = time.time()
            # 推送通知
            BG_NOTIFICATIONS.push(f"Task {task_id} completed", priority="high", key=task_id)
        except Exception as e:
            with self._lock:
                self.tasks[task_id]["status"] = "failed"
                self.tasks[task_id]["result"] = str(e)

def run_subagent_background(prompt: str) -> str:
    task_id = str(uuid.uuid4())[:8]
    thread = threading.Thread(target=run_subagent, args=(prompt, task_id), daemon=True)
    thread.start()
    return f"Subagent started with task_id: {task_id}"
```

### 与前一章的对比

| 特性 | Capiter 12 | Capiter 13 |
|------|------------|------------|
| 子代理执行 | 串行阻塞 | 后台并行 |
| 通知机制 | 无 | 优先级队列 |
| 任务查询 | 无 | check_background |
| 停滞检测 | 无 | detect_stalled |

### 架构图

```
┌───────────────┐
│  Main Agent   │
│  agent_loop   │
└───────┬───────┘
        │ background_task()
        ▼
┌───────────────┐     ┌─────────────┐
│ BackgroundMgr │ ──> │  Thread 1   │
│               │     │ (subagent)  │
│               │     └──────┬──────┘
│               │            │
│               │     ┌──────┴──────┐
│               │     │  Thread 2   │
│               │     │ (subagent)  │
│               │     └──────┬──────┘
│               │            │
│               │     ┌──────▼──────┐
│               │     │ Notification│
│               │     │   Queue     │
│               │     └──────┬──────┘
└───────────────┘            │
        ▲                    │ drain()
        └────────────────────┘
```

### 后台任务执行流程

```
1. 主代理调用 background_task(prompt)
2. BackgroundManager 创建任务记录
3. 启动后台线程执行子代理
4. 立即返回 task_id 给主代理
5. 主代理继续其他工作
6. 子代理完成后：
   a. 更新任务状态为 completed
   b. 推送通知到队列
7. 主循环 drain 通知队列
8. 注入通知到对话上下文
```

### 本章小结

Capiter 13 引入后台任务执行能力，子代理可在后台线程并行运行。通知队列支持优先级和消息折叠，主循环定期 drain 获取完成通知。停滞检测防止任务无限期挂起。异步执行模型提高了框架的并发能力。

---

## Capiter 14: Cron 调度器

**文件路径**: `code/v1_task_manager/chapter_14/s14_cron_scheduler.py`  
**完整分析**: `docs/zh/chapter_14/s14_cron_scheduler_文档.md`

### 核心组件

| 组件 | 类型 | 说明 |
|------|------|------|
| `CronScheduler` | class | Cron 定时任务调度器 |
| `CronLock` | class | 基于 PID 文件的锁（防止重复触发） |
| `cron_matches()` | function | Cron 表达式匹配函数 |
| `SCHEDULED_TASKS_FILE` | const | 定时任务配置文件路径 |
| `check_due()` | method | 检查到期任务 |
| `execute_task()` | method | 执行定时任务 |

### 功能改动详情

1. **Cron 表达式支持**
   - 标准 5 字段格式：`minute hour day month day_of_week`
   - 支持特殊字符：`*`（任意）、`*/n`（每 n）、`n,m`（多值）、`n-m`（范围）
   - 示例：`0 30 * * *`（每 30 分钟）、`0 2 * * *`（每天 2 点）

2. **CronLock 防重复触发**
   - 基于 PID 文件的锁机制
   - 任务执行前获取锁，执行后释放
   - 防止同一任务多次并发执行

3. **任务持久化**
   - 任务配置保存在 `cron_state.json`
   - 记录最后执行时间和下次计划执行时间
   - 支持重启后恢复调度状态

4. **调度器循环**
   - 定期检查到期任务（默认每分钟）
   - `check_due()` 比对当前时间与 Cron 表达式
   - `execute_task()` 执行任务并更新状态

5. **与后台任务集成**
   - 定时任务可调用 `background_task()` 执行
   - 支持长时间运行的定时作业
   - 任务完成通知推送到队列

```python
def cron_matches(cron_expr: str, dt: datetime) -> bool:
    """Check if datetime matches cron expression"""
    minute, hour, day, month, dow = cron_expr.split()
    
    matches_minute = _match_field(minute, dt.minute, 0, 59)
    matches_hour   = _match_field(hour, dt.hour, 0, 23)
    matches_day    = _match_field(day, dt.day, 1, 31)
    matches_month  = _match_field(month, dt.month, 1, 12)
    matches_dow    = _match_field(dow, dt.weekday(), 0, 6)
    
    return all([matches_minute, matches_hour, matches_day, matches_month, matches_dow])

class CronLock:
    def __init__(self, task_name: str):
        self.lock_file = LOCK_DIR / f"{task_name}.pid"
    
    def acquire(self) -> bool:
        if self.lock_file.exists():
            old_pid = int(self.lock_file.read_text().strip())
            if self._is_running(old_pid):
                return False  # 已有实例在运行
        self.lock_file.write_text(str(os.getpid()))
        return True
    
    def release(self):
        if self.lock_file.exists():
            self.lock_file.unlink()

class CronScheduler:
    def check_due(self, task: dict) -> bool:
        now = datetime.now()
        last_run = task.get("last_run")
        if last_run and (now - datetime.fromtimestamp(last_run)).seconds < 60:
            return False  # 1 分钟内不重复检查
        return cron_matches(task["cron"], now)
    
    def execute_task(self, task: dict):
        lock = CronLock(task["name"])
        if not lock.acquire():
            return  # 已有实例在运行
        try:
            # 执行任务（可能是 background_task）
            background_task(task["command"])
            self.update_state(task["name"], time.time())
        finally:
            lock.release()
```

### 与 Capiter 13 的对比

| 特性 | Capiter 13 | Capiter 14 |
|------|------------|------------|
| 触发方式 | 手动调用 | 定时自动 |
| 任务来源 | 运行时传入 | 预定义配置文件 |
| 并发控制 | 线程锁 | CronLock (PID 文件) |
| 状态持久化 | 内存 | cron_state.json |

### 架构图

```
┌─────────────────────────────────────────────────────┐
│                CronScheduler Loop                   │
│         (check_due(), execute_task())               │
└─────────────────────┬───────────────────────────────┘
                      │
        ┌─────────────┼─────────────┐
        │             │             │
        ▼             ▼             ▼
   ┌─────────┐  ┌─────────┐  ┌─────────┐
   │ 0 30 *  │  │ 0 2 * * │  │ 0 0 1 * │
   │ * * *   │  │ * * *   │  │ * * *   │
   │ hourly  │  │ daily   │  │ monthly │
   └────┬────┘  └────┬────┘  └────┬────┘
        │            │            │
        ▼            ▼            ▼
   .cron/lock/     .cron/lock/     .cron/lock/
   hourly.pid      daily.pid       monthly.pid
```

### Cron 表达式匹配逻辑

```python
def _match_field(field: str, value: int, min_val: int, max_val: int) -> bool:
    """Match single cron field against value."""
    if field == "*":
        return True
    if field.startswith("*/"):
        step = int(field[2:])
        return value % step == 0
    if "," in field:
        return value in [int(x) for x in field.split(",")]
    if "-" in field:
        start, end = map(int, field.split("-"))
        return start <= value <= end
    return value == int(field)
```

### 本章小结

Capiter 14 引入定时任务调度器，支持标准 Cron 表达式。CronLock 防止重复触发，状态持久化支持重启恢复。调度器定期检查到期任务并执行，与后台任务系统集成。

---

## Capiter 18_2: Worktree 隔离

**文件路径**: `code/v1_task_manager/chapter_18_2/s18_v2_worktree.py`  
**完整分析**: `docs/zh/chapter_18_2/s18_v2_worktree_文档.md`

### 核心组件

| 组件 | 类型 | 说明 |
|------|------|------|
| `worktree_create` | function | 创建 Git Worktree 隔离执行环境 |
| `worktree_list` | function | 列出所有 tracked worktrees |
| `worktree_enter` | function | 进入或重新打开 worktree lane |
| `worktree_status` | function | 显示 git status for one worktree |
| `worktree_run` | function | 在 named worktree directory 运行 shell 命令 |
| `worktree_closeout` | function | Close out worktree lane (keep/remove) |
| `task_bind_worktree` | function | 绑定 task 到 worktree name |
| `.worktrees/index.json` | file | Worktree 状态持久化 |

### 功能改动详情

1. **Worktree 创建与 Git 分支管理**
   - 基于 Git worktree 机制创建隔离执行环境
   - 支持从指定 base_ref 分支
   - 自动命名规范（1-40 chars: letters, digits, ., _, -）

2. **任务-Worktree 绑定机制**
   - `task_bind_worktree(task_id, worktree, owner)` 
   - 设置 task 的 worktree_state 为 'active'
   - 支持可选的 owner 参数

3. **Worktree 隔离执行**
   - `worktree_run(name, command)` 在隔离目录执行
   - 每个 worktree 有独立的 git 状态
   - 支持并行或 risky work

4. **生命周期管理**
   - `worktree_closeout(name, action, reason, force, complete_task)`
   - action: 'keep' 保留或 'remove' 删除
   - 可选 force remove（即使有 uncommitted changes）
   - 可选 complete_task（标记 bound task 完成）

5. **状态持久化与事件追踪**
   - `.worktrees/index.json` 存储 worktree 元数据
   - `.worktrees/events.jsonl` 记录 lifecycle events
   - `worktree_events(limit)` 查询最近事件

```python
# Worktree 创建示例
def worktree_create(name, task_id=None, base_ref="HEAD"):
    """Create git worktree execution lane"""
    # 验证 name 格式
    # 创建 worktree 目录
    # 更新 .worktrees/index.json
    # 可选绑定 task_id
```

### 与 Capiter 17 的对比

| 维度 | Capiter 17 | Capiter 18_2 | 变化说明 |
|------|------------|--------------|----------|
| 执行环境 | 单一目录 | 多 worktree 隔离 | 支持并行任务 |
| Git 状态 | 共享 | 独立 | 避免冲突 |
| 任务绑定 | 无 | worktree_state | 明确执行上下文 |
| 持久化 | 内存 | index.json + events.jsonl | 可恢复 |

### 架构图

```
┌─────────────────────────────────────────────────────┐
│              Main Planner Agent                     │
│  (task_create, task_bind_worktree, task_update)    │
└─────────────────────┬───────────────────────────────┘
                      │
        ┌─────────────┼─────────────┐
        │             │             │
        ▼             ▼             ▼
   ┌─────────┐  ┌─────────┐  ┌─────────┐
   │ WT: fea │  │ WT: fix │  │ WT: exp │
   │  ture1 │  │  _bug1  │  │  eriment│
   └────┬────┘  └────┬────┘  └────┬────┘
        │            │            │
        ▼            ▼            ▼
   .worktrees/index.json (状态持久化)
   .worktrees/events.jsonl (事件日志)
```

### 本章小结

- **核心变化**: 从单目录执行 → Git Worktree 隔离执行
- **架构改进**: 每个 task 可绑定独立 worktree，支持并行开发
- **新增机制**: worktree 生命周期管理、事件追踪
- **职责分离**: Main Planner 负责任务分配，worktree 负责隔离执行

---

## Capiter 19_2: MCP 插件（v2 MCP Plugin）

**文件路径**: `code/v1_task_manager/chapter_19_2/s19_v2_mcp_plugin.py`  
**完整分析**: `docs/zh/chapter_19_2/s19_mcp_plugin.md`

### 核心组件

| 组件 | 类型 | 说明 |
|------|------|------|
| `MCPPlugin` | class | MCP 插件管理类 |
| `mcp__{server}__{tool}` | naming | MCP 工具命名规范 |
| MCP 服务器连接 | connection | 管理外部 MCP server 连接 |
| 工具发现 | discovery | 动态加载外部工具 |
| 工具注册 | registry | 注册 MCP tools 到 agent |

### 功能改动详情

1. **MCP 协议集成**
   - Model Context Protocol (MCP) 标准化接口
   - 支持连接外部 MCP 服务器
   - 统一的工具调用规范

2. **外部工具命名规范**
   - 格式：`mcp__{server}__{tool}`
   - 示例：`mcp__github__list_issues`, `mcp__slack__send_message`
   - 避免与原生工具命名冲突

3. **MCP 服务器连接生命周期**
   - 启动时建立连接
   - 运行时保持长连接
   - 异常时自动重连机制

4. **工具发现与注册机制**
   - 连接时自动 discovery 可用 tools
   - 动态注册到 agent 的工具列表
   - 支持 tool schema 自动获取

5. **与原生工具的协同工作**
   - MCP tools 与原生 tools 统一调度
   - `execute_tool_calls()` 透明处理
   - 无感知切换

```python
# MCP 工具调用示例
response = client.chat.completions.create(
    model=MODEL,
    tools=TOOLS + MCP_TOOLS,  # 合并原生和 MCP 工具
    messages=state.messages
)

# 执行时根据命名前缀区分
if tool_name.startswith("mcp__"):
    result = call_mcp_tool(tool_name, args)
else:
    result = call_native_tool(tool_name, args)
```

### 与 Capiter 18_2 的对比

| 维度 | Capiter 18_2 | Capiter 19_2 | 变化说明 |
|------|--------------|--------------|----------|
| 扩展方式 | Git Worktree | MCP 协议 | 从隔离到集成 |
| 工具来源 | 内置 | 外部服务器 | 支持第三方 |
| 命名规范 | 简单名称 | `mcp__{server}__{tool}` | 避免冲突 |
| 生命周期 | 手动管理 | 自动重连 | 更健壮 |

### 架构图

```
┌─────────────────────────────────────────────────────┐
│                    Agent Loop                       │
│              execute_tool_calls()                   │
└─────────────────────┬───────────────────────────────┘
                      │
        ┌─────────────┴─────────────┐
        │                           │
        ▼                           ▼
┌───────────────┐          ┌───────────────┐
│ Native Tools  │          │  MCP Plugin   │
│ - bash        │          │               │
│ - read_file   │          │  mcp__github  │
│ - write_file  │          │  mcp__slack   │
│ - task_*      │          │  mcp__jira    │
└───────────────┘          └───────┬───────┘
                                   │
                          ┌────────┴────────┐
                          ▼                 ▼
                    ┌──────────┐     ┌──────────┐
                    │  GitHub  │     │  Slack   │
                    │   API    │     │   API    │
                    └──────────┘     └──────────┘
```

### 本章小结

- **核心变化**: 从封闭工具集 → 开放 MCP 生态集成
- **架构改进**: `mcp__{server}__{tool}` 命名规范，统一调度
- **新增能力**: 支持第三方服务（GitHub, Slack, Jira 等）
- **扩展性**: 无需修改核心代码即可接入新服务

---

## 文档总结

### 完整章节列表（16 章）

| 章节 | 主题 | 核心变化 |
|------|------|----------|
| 1 | 基础 Agent Loop | 单一 bash 工具 + 简洁循环 |
| 2 | 工具系统扩展 | 4 个文件操作工具 |
| 3 | 技能系统引入 | SkillRegistry + load_skill |
| 4 | 任务管理系统 | Todo 持久化 |
| 5 | 子代理系统 | 主/子代理职责分离 |
| 6 | 上下文管理 | 消息历史压缩 |
| 7 | 权限系统 | 文件操作权限检查 |
| 8 | Hook 系统 | 前置/后置 Hook |
| 9 | 记忆系统 | MemoryStore 持久化 |
| 10 | 构建系统 | 构建流程自动化 |
| 11 | 恢复系统 | 会话保存与加载 |
| 12 | 任务系统增强 | 依赖管理 + 批量操作 |
| 13 | v2 后台任务 | 异步执行 + 结果回收 |
| 14 | Cron 调度器 | 定时任务调度 |
| 18_2 | Worktree 隔离 | Git Worktree 并行执行 |
| 19_2 | MCP 插件 | 外部服务集成 |

### 架构演进路线

```
Capter 1-3:   基础能力（Agent Loop → 工具 → 技能）
    ↓
Capter 4-7:   任务与协作（Todo → Subagent → Context → Permission）
    ↓
Capter 8-12:  增强系统（Hook → Memory → Build → Resume → Task v2）
    ↓
Capter 13-14: 异步与调度（Backtask → Cron）
    ↓
Capter 18_2-19_2: 扩展与集成（Worktree → MCP）
```

---

**文档版本**: v2  
**总行数**: 约 2500 行  
**最后更新**: 2026-04-22